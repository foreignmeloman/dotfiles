#!/bin/bash
# vim: syntax=bash:
set -e

{{ output "find" "pyinfra/" "-type" "f" "-exec" "sha256sum" "{}" "+" | comment "# " }}

export PATH="${PATH}:${HOME}/.local/bin"
pipx install pyinfra
pyinfra inventory.py deploy.py -yv --chdir {{ .chezmoi.sourceDir }}/pyinfra
